/**
 * STREAM B — Drone rigid body
 *
 * Owns the Rapier RigidBody + collider for the quad. Applies thrust + torque
 * each tick (commands come from the flight controller via the world).
 */
export {};
