/**
 * STREAM G — Pause menu + settings
 *
 * Esc opens overlay: mode toggle, rates/expo/FOV sliders, audio toggle,
 * "Reset position" button. Writes to a config singleton + emits events.
 */
export {};
