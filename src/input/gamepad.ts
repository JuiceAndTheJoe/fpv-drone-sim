/**
 * STREAM D — Gamepad input
 *
 * Standard mapping (Mode 2):
 *   axes[0]  left stick X  -> yaw
 *   axes[1]  left stick Y  -> throttle (inverted)
 *   axes[2]  right stick X -> roll
 *   axes[3]  right stick Y -> pitch (inverted)
 *   buttons[8] (Select)    -> mode toggle
 *   buttons[9] (Start)     -> reset
 */
export {};
