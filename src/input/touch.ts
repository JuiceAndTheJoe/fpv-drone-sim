/**
 * STREAM D — Touch input
 *
 * Two virtual sticks rendered as canvas overlays:
 *   left  (~30vh circle, bottom-left)  -> throttle (Y) + yaw (X)
 *   right (~30vh circle, bottom-right) -> pitch (Y) + roll (X)
 *
 * Auto-hidden on devices without a coarse pointer.
 */
export {};
