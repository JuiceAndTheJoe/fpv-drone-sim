/**
 * STREAM D — Keyboard input
 *
 * Default mapping:
 *   W/S  pitch (down/up — push stick forward to dive)
 *   A/D  roll
 *   Q/E  yaw
 *   Shift / Space  throttle up (hold)
 *   Ctrl  throttle down
 *   R     reset
 *   Tab   mode toggle
 */
export {};
