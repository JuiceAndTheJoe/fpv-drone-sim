/**
 * STREAM F — Race track + gates
 * Owner: TBD
 *
 * Builds a sequence of gate meshes, owns the lap state machine, emits
 * 'gateCleared' and 'lapComplete' events as the drone passes through.
 */
export {};
