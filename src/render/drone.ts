/**
 * STREAM E — Drone mesh + prop spin
 *
 * Loads quad.glb when present; otherwise builds a procedural placeholder
 * (body box + 4 spinning prop discs). Reads state.rpm to drive prop speed.
 */
export {};
