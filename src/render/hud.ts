/**
 * STREAM E — HUD overlay (DOM, not WebGL)
 *
 * Throttle bar, mode badge, FPS counter, lap timer, next-gate indicator.
 * Owns its own root <div id="hud"> appended next to the canvas.
 */
export {};
